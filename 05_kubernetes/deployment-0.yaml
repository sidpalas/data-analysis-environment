---
apiVersion: "extensions/v1beta1"
kind: "Deployment"
metadata:
  name: "jupyter-vscode"
  namespace: "default"
  labels:
    app: "jupyter-vscode"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "jupyter-vscode"
  template:
    metadata:
      labels:
        app: "jupyter-vscode"
    spec:
      volumes:
      - name: notebook-pv-storage
        persistentVolumeClaim:
          claimName: notebook-pv-claim
      containers:
      - name: "jupyter-vscode"
        securityContext:
          privileged: true
        image: "gcr.io/jupyter-244404/jupyter-vscode:latest"
        stdin: true
        tty: true
        volumeMounts:
        - mountPath: "/home/notebooks"
          name: notebook-pv-storage
---
# persistent volume claim
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: notebook-pv-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
